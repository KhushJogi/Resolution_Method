kb(knowledge base) and query  files are the inputs for the resolution method.

kb file given is in clause form.

Notation - 

kb1.xml denotes the file without equality axioms added.
kb1_eq.xml denotes the files with equality axioms added.
query1.xml denotes the query for both kb1 and kb1_eq.

Please generate the output for both cases (kb1, query1 ) and (kb1_eq and query1).

The algorithm implemented by u should output either a substitution or
FALSE, along with a trace in a text file.

For the better understanding , one output file is also given.

output3.txt
This output is generated when the input is taken as kb3.xml and query3.xml .
